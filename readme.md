Features to debug the Image Source Control plugin

Once installed and enabled, you will see this:

- ISC Debug menu item in the admin bar in the frontend – lists images that ISC found on the given page with information about the source text and links to the image on the page and to the edit page in the backend